<!--
  Generated by promptc dev
  Source: team-service.spec.promptc
  Compiled: 2026-03-20T15:57:01Z
  Do not edit this file directly — edit the spec and recompile.
-->

# Project Instructions

## Context

- **audience**: customer operations
- **language**: typescript
- **runtime**: bun
- **service_tier**: internal tooling

## Guidelines

# REST API Best Practices

When building REST endpoints:

## HTTP Methods
- Use GET for reading data (idempotent, no side effects)
- Use POST for creating new resources
- Use PUT for full updates of existing resources
- Use PATCH for partial updates
- Use DELETE for removing resources

## Status Codes
- Return 200 for successful GET, PUT, PATCH
- Return 201 for successful POST (resource created)
- Return 204 for successful DELETE (no content)
- Return 400 for bad requests (invalid input)
- Return 401 for authentication required
- Return 403 for forbidden (authenticated but not authorized)
- Return 404 for resource not found
- Return 500 for internal server errors

## Best Practices
- Include request IDs in headers for debugging and tracing
- Validate all inputs on the server side
- Use meaningful resource names (plural nouns)
- Version your API (e.g., /api/v1/resource)
- Return consistent error response format
- Use pagination for list endpoints
- Include rate limiting headers


# Security Constraints

## Authentication & Authorization
- Never hardcode credentials, API keys, or secrets
- Use environment variables for sensitive configuration
- Implement proper authentication for all protected endpoints
- Use principle of least privilege for authorization
- Implement session timeout and token expiration

## Input Validation
- Validate and sanitize all user inputs
- Use parameterized queries to prevent SQL injection
- Escape output to prevent XSS attacks
- Validate file uploads (type, size, content)
- Implement rate limiting to prevent abuse

## Data Protection
- Encrypt sensitive data at rest
- Use TLS/HTTPS for data in transit
- Never log sensitive information (passwords, tokens, PII)
- Implement secure password hashing (bcrypt, argon2)
- Follow GDPR/privacy regulations for user data

## Error Handling
- Don't expose internal errors to users
- Log errors securely for debugging
- Use generic error messages for authentication failures
- Implement proper CORS policies


# Team API Standards

## Delivery Expectations
- Design endpoints for operational support workflows rather than generic CRUD alone.
- Include idempotency guidance for actions that may be retried by support staff.
- Prefer structured error codes that can be surfaced directly in internal tooling.

## Observability
- Emit request IDs in every response and log entry.
- Capture actor identity and escalation reason in audit events.

## Collaboration
- Document ownership for each workflow before implementation starts.
- Keep runbooks close to the feature when the workflow can affect customer operations.

## Features

- **intake_dashboard**: Triage incoming support requests with saved views and routing shortcuts
- **escalation_notes**: Generate structured escalation notes for engineering handoff
- **audit_log**: Keep a searchable audit trail of operator actions

## Constraints

- No Hardcoded Secrets
- Pii Redaction
- Accessible Ui Copy
